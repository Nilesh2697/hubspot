"use strict";
/* hs-eslint ignored failing-rules */
/* eslint-disable hubspot-dev/no-unsupported-ts-syntax */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildServiceConfiguration = exports.loadFunctionInfo = exports.loadMultiFunctionInfo = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const constants_1 = require("./constants");
// @ts-expect-error type declaration not available
const http_1 = __importDefault(require("@hubspot/cli-lib/http"));
// @ts-expect-error type declaration not available
const cli_lib_1 = require("@hubspot/cli-lib");
const errors_1 = require("./errors");
function loadMultiFunctionInfo(config) {
    const srcDir = path_1.default.join(config.app.path, constants_1.APP_FUNCTIONS_DIRNAME);
    if (!fs_1.default.existsSync(srcDir)) {
        throw new Error(`Cannot locate app function directory: ${srcDir}`);
    }
    const stats = fs_1.default.lstatSync(srcDir);
    if (!stats.isDirectory()) {
        throw new Error(`Invalid app function directory: ${srcDir}`);
    }
    const manifestPath = path_1.default.join(srcDir, constants_1.APP_FUNCTIONS_MANIFEST_FILENAME);
    const manifest = JSON.parse(fs_1.default.readFileSync(manifestPath, {
        encoding: 'utf-8',
    }));
    const { appFunctions = {} } = manifest;
    // TODO validation
    return { appFunctions, srcDir };
}
exports.loadMultiFunctionInfo = loadMultiFunctionInfo;
function loadFunctionInfo(config, functionName, appId) {
    const { logger } = config;
    const { appFunctions, srcDir } = loadMultiFunctionInfo(config);
    const metadata = appFunctions[functionName];
    if (!metadata) {
        const manifestPath = path_1.default.join(srcDir, constants_1.APP_FUNCTIONS_MANIFEST_FILENAME);
        logger.error(`App function "${functionName}" is not declared in ${manifestPath}`);
        throw new errors_1.ExecutionError({
            reason: errors_1.Reason.FunctionNotFound,
            functionName,
            appId,
        });
    }
    // TODO validation
    return Object.assign(Object.assign({}, metadata), { srcDir, name: functionName });
}
exports.loadFunctionInfo = loadFunctionInfo;
function buildServiceConfiguration(appPath, options) {
    var _a, _b, _c, _d, _e, _f, _g;
    const accountId = (_a = options.accountId) !== null && _a !== void 0 ? _a : (0, cli_lib_1.getAccountId)();
    const hubspotApiOrigin = (_b = options.hubspotApiOrigin) !== null && _b !== void 0 ? _b : getHubSpotApiOrigin(accountId);
    const hubspotWebsiteOrigin = (_c = options.hubspotWebsiteOrigin) !== null && _c !== void 0 ? _c : getHubSpotWebsiteOrigin(accountId);
    return {
        accountId,
        app: { path: appPath },
        functionTimeoutMs: (_d = options.functionTimeoutMs) !== null && _d !== void 0 ? _d : constants_1.EXECUTION_TIMEOUT_MS,
        httpClient: (_e = options.httpClient) !== null && _e !== void 0 ? _e : http_1.default,
        hubspotApiOrigin,
        hubspotWebsiteOrigin,
        logger: (_f = options.logger) !== null && _f !== void 0 ? _f : console,
        platformVersion: (_g = options.platformVersion) !== null && _g !== void 0 ? _g : constants_1.PLATFORM_VERSION.V20231,
    };
}
exports.buildServiceConfiguration = buildServiceConfiguration;
/**
 * Use the CLI lib to determine the HubSpot API base URL for the given account.
 * This supports QA and PROD accounts.
 * @param accountId
 * @returns
 */
function getHubSpotApiOrigin(accountId) {
    const { env } = (0, cli_lib_1.getAccountConfig)(accountId);
    const { baseUrl } = http_1.default.getRequestOptions({ env });
    return baseUrl;
}
/**
 * Use the CLI lib to determine the HubSpot App base URL for the given account.
 * This supports QA and PROD accounts.
 * @param accountId
 * @returns
 */
function getHubSpotWebsiteOrigin(accountId) {
    const { env } = (0, cli_lib_1.getAccountConfig)(accountId);
    const envUrlString = (env === null || env === void 0 ? void 0 : env.toLowerCase()) === 'qa' ? 'qa' : '';
    return `https://app.hubspot${envUrlString}.com`;
}
//# sourceMappingURL=config.js.map